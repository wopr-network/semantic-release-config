# @wopr-network/semantic-release-config

Shared semantic-release config for all wopr-network plugin repos.

## Usage

Install: `npm install --save-dev semantic-release @wopr-network/semantic-release-config`

In `release.config.js`:
```js
module.exports = require('@wopr-network/semantic-release-config');
```
